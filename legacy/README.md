# Legacy prototypes

These notebooks are the original exploratory prototypes that predate the
`mpph` package. They use the retired `list/organism` endpoint, duplicated
inline code, and the old plotting approach.

**Archived for history — not recommended for current analyses.** Use the
`mpph` package (see the top-level `README.md`) instead.
